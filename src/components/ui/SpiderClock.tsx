import React from 'react';

// Simple placeholder component kept for compatibility (no GSAP, minimal clock handled in dashboard)
const SpiderClock: React.FC = () => {
  return <div style={{ padding: 20 }}>Clock removed — using simpler built-in clock.</div>;
};

export default SpiderClock;
