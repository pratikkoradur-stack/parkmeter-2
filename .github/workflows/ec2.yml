name: Deploy to Ubuntu EC2

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Deploy files to EC2
        uses: easingthemes/ssh-deploy@v4
        with:  # ✅ CHANGED: Use 'with:' instead of 'env:'
          SSH_PRIVATE_KEY: ${{ secrets.EC2_SSH_KEY }}
          REMOTE_HOST: ${{ secrets.EC2_IP }}
          REMOTE_USER: ${{ secrets.EC2_USERNAME }}
          TARGET: ${{ secrets.TARGET_DIR }}
          SOURCE: ./
          EXCLUDE: '.git/, .github/, node_modules/, .env, README.md'
          ARGS: '-r --delete'

      - name: Install and configure web server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.EC2_IP }}
          username: ${{ secrets.EC2_USERNAME }}
          key: ${{ secrets.EC2_SSH_KEY }}
          script: |
            # Update system packages
            echo "🔄 Updating system packages..."
            sudo apt-get update -y
            
            # Install Apache
            echo "📦 Installing Apache..."
            sudo apt-get install -y apache2
            
            # Create web root if it doesn't exist
            sudo mkdir -p /var/www/html
            
            # Copy deployed files to web root
            echo "📁 Copying files to web root..."
            sudo cp -r /home/ubuntu/app/* /var/www/html/ 2>/dev/null || true
            sudo cp -r /home/ubuntu/app/.* /var/www/html/ 2>/dev/null || true
            
            # Set proper permissions
            echo "🔒 Setting permissions..."
            sudo chown -R www-data:www-data /var/www/html
            sudo chmod -R 755 /var/www/html
            sudo find /var/www/html -type f -exec chmod 644 {} \;
            
            # Configure Apache to serve your files
            echo "⚙️ Configuring Apache..."
            sudo systemctl enable apache2
            sudo systemctl restart apache2
            
            # Check if Apache is running
            if sudo systemctl is-active --quiet apache2; then
              echo "✅ Apache is running successfully!"
            else
              echo "❌ Apache failed to start"
              sudo systemctl status apache2
              exit 1
            fi
            
            echo "🎉 Deployment completed successfully!"
            echo "🌐 Your website should be available at: http://${{ secrets.EC2_IP }}"
